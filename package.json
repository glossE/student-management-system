{
  "name": "student-management-system",
  "version": "1.0.0",
  "description": "Full-stack Student Management System — Angular 19 + PrimeNG, Express, Prisma, PostgreSQL, Cloudinary",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install",
    "build": "npm run install:all && npm --prefix frontend run build && node scripts/copy-dist.js && npm --prefix backend run prisma:generate",
    "start": "npm --prefix backend run start:prod",
    "dev:backend": "npm --prefix backend run dev",
    "dev:frontend": "npm --prefix frontend start",
    "seed": "npm --prefix backend run seed"
  }
}
