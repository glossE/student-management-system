{
  "name": "student-management-system",
  "version": "1.0.0",
  "description": "Full-stack Student Management System — Angular 19 + PrimeNG, Express, Prisma, PostgreSQL, Cloudinary",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "install:all": "npm run install:backend && npm run install:frontend",
    "install:backend": "cd backend && npm install",
    "install:frontend": "cd frontend && npm install",
    "build": "npm run install:all && npm run build:frontend && node scripts/copy-dist.js && npm run generate:prisma",
    "build:frontend": "cd frontend && npm run build",
    "generate:prisma": "cd backend && npm run prisma:generate",
    "start": "cd backend && npm run start:prod",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "seed": "cd backend && npm run seed"
  }
}
